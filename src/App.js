import { useState } from "react";

function App() {
    const [loading, setLoading] = useState(true); // default
    // 첫 번째 item이 data, 두 번째는 dat를 수정할 수 있는 함수
    return <div></div>
};

export default App;